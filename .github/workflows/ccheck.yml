name: ccheck

on: [push, pull_request]

jobs:
  ccheck:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: ccheck
        run: make ccheck
      - name: CMakeLint
        run: |
          pip install cmakelint
          ~/.local/bin/cmakelint
